# Diabetes Prediction using ML

This directory contains a machine learning project for predicting diabetes.

## Project Overview

The main notebook `main.ipynb` includes the code for data preprocessing, model training, and evaluation for predicting the onset of diabetes.
